diff --git a/src/overlays/gamestates/ovl_file_choose/z_file_nameset.c b/src/overlays/gamestates/ovl_file_choose/z_file_nameset.c
index d80dd7bc9..72802f8cc 100644
--- a/src/overlays/gamestates/ovl_file_choose/z_file_nameset.c
+++ b/src/overlays/gamestates/ovl_file_choose/z_file_nameset.c
@@ -16,6 +16,7 @@
 #include "versions.h"
 #include "audio.h"
 #include "save.h"
+#include "z_lib.h"
 
 #include "assets/textures/title_static/title_static.h"
 
@@ -46,7 +47,7 @@ void FileSelect_DrawCharacterTransition(GraphicsContext* gfxCtx, void* texture1,
 #if OOT_PAL
 
 #if OOT_VERSION >= PAL_1_1
-s16 D_808124C0[] = {
+s16 fileNameCharWidth[] = {
     2, 3, 2, 2, 2, 2, 2, 2, 2, 2, 1, 2, 0, 1, 1, 2, 1, 1, 4, 2, 2, 2, 1, 1, 0, 2, 0, 1, 1, 1, 1, 1, 0,
     1, 1, 1, 2, 2, 2, 2, 2, 3, 2, 2, 4, 3, 2, 4, 1, 2, 2, 1, 1, 2, 2, 3, 2, 2, 0, 2, 2, 2, 0, 3, 1, 0,
 };
@@ -178,12 +179,16 @@ static void* sButtonTextures[][2] = {
 static u16 sButtonWidths[] = { 28, 44 };
 #endif
 
-static s16 D_808125EC[] = {
-    0xFFE2, 0xFFF0, 0xFFFA, 0x0004, 0x000E, 0x0018, 0x0022, 0x002C, 0x0036, 0xFFF0, 0xFFF0,
+static s16 NameCharPosX[] = {
+    -26, -16, -6, 4, 14, 24, 34, 44, 54, 64, 74, 84,
+};
+
+static s16 SeedCharPosX[] = {
+    -26, -17, -8, 1, 10, 19, 28, 37, 46, 55, 64, 73,
 };
 
 static s16 D_80812604[] = {
-    0x0048, 0x0045, 0x0045, 0x0045, 0x0045, 0x0045, 0x0045, 0x0045, 0x0045, 0x0045, 0x0045,
+    0x0048, 0x0045, 0x0045, 0x0045, 0x0045, 0x0045, 0x0045, 0x0045, 0x0045, 0x0045, 0x0045, 0x0045, 0x0045
 };
 
 /**
@@ -205,12 +210,19 @@ void FileSelect_SetNameEntryVtx(GameState* thisx) {
     gDPPipeSync(POLY_OPA_DISP++);
     gDPSetCombineLERP(POLY_OPA_DISP++, PRIMITIVE, ENVIRONMENT, TEXEL0, ENVIRONMENT, TEXEL0, 0, PRIMITIVE, 0, PRIMITIVE,
                       ENVIRONMENT, TEXEL0, ENVIRONMENT, TEXEL0, 0, PRIMITIVE, 0);
-    gDPSetPrimColor(POLY_OPA_DISP++, 0, 0, 255, 255, 255, this->titleAlpha[0]);
     gDPSetEnvColor(POLY_OPA_DISP++, 0, 0, 0, 0);
     gSPVertex(POLY_OPA_DISP++, gNameEntryVtx, 24, 0);
-    gDPLoadTextureBlock(POLY_OPA_DISP++, sNameLabelTextures[gSaveContext.language], G_IM_FMT_IA, G_IM_SIZ_8b, 56, 16, 0,
-                        G_TX_NOMIRROR | G_TX_WRAP, G_TX_NOMIRROR | G_TX_WRAP, G_TX_NOMASK, G_TX_NOMASK, G_TX_NOLOD,
-                        G_TX_NOLOD);
+    if (this->configMode >= CM_RANDOMIZER_TO_SEED_ENTRY) {
+        gDPSetPrimColor(POLY_OPA_DISP++, 0, 0, 255, 255, 255, 255);
+        gDPLoadTextureBlock(POLY_OPA_DISP++, gRandoSeedPromptENGTex, G_IM_FMT_IA, G_IM_SIZ_8b, 56, 16, 0,
+                            G_TX_NOMIRROR | G_TX_WRAP, G_TX_NOMIRROR | G_TX_WRAP, G_TX_NOMASK, G_TX_NOMASK, G_TX_NOLOD,
+                            G_TX_NOLOD);
+    } else {
+        gDPSetPrimColor(POLY_OPA_DISP++, 0, 0, 255, 255, 255, this->titleAlpha[0]);
+        gDPLoadTextureBlock(POLY_OPA_DISP++, sNameLabelTextures[gSaveContext.language], G_IM_FMT_IA, G_IM_SIZ_8b, 56, 16, 0,
+                            G_TX_NOMIRROR | G_TX_WRAP, G_TX_NOMIRROR | G_TX_WRAP, G_TX_NOMASK, G_TX_NOMASK, G_TX_NOLOD,
+                            G_TX_NOLOD);
+    }
     gSP1Quadrangle(POLY_OPA_DISP++, 0, 2, 3, 1, 0);
     gDPPipeSync(POLY_OPA_DISP++);
 
@@ -253,32 +265,39 @@ void FileSelect_SetNameEntryVtx(GameState* thisx) {
     }
 #endif
 
-    this->nameEntryVtx = GRAPH_ALLOC(this->state.gfxCtx, 44 * sizeof(Vtx));
+    this->nameEntryVtx = GRAPH_ALLOC(this->state.gfxCtx, 52 * sizeof(Vtx));
 
-    for (phi_s0 = 0, phi_t1 = 0; phi_t1 < 44; phi_t1 += 4, phi_s0++) {
+    u8* entryArr = &this->fileNames[this->buttonIndex][0];
+    s16* xPos = NameCharPosX;
+    if (this->configMode >= CM_RANDOMIZER_TO_SEED_ENTRY) {
+        entryArr = &this->seedChars[this->buttonIndex][0];
+        xPos = SeedCharPosX;
+    }
+
+    for (phi_s0 = 0, phi_t1 = 0; phi_t1 < 52; phi_t1 += 4, phi_s0++) {
 #if OOT_NTSC
         this->nameEntryVtx[phi_t1].v.ob[0] = this->nameEntryVtx[phi_t1 + 2].v.ob[0] =
-            D_808125EC[phi_s0] + this->nameEntryBoxPosX;
+            NameCharPosX[phi_s0] + this->nameEntryBoxPosX;
 
         this->nameEntryVtx[phi_t1 + 1].v.ob[0] = this->nameEntryVtx[phi_t1 + 3].v.ob[0] =
             this->nameEntryVtx[phi_t1].v.ob[0] + 0xA;
 #else
-        if ((phi_s0 > 0) && (phi_s0 < 9)) {
+        if ((phi_s0 > 0) && (phi_s0 < 11)) {
 #if OOT_VERSION >= PAL_1_1
-            temp = this->fileNames[this->buttonIndex][phi_s0 - 1];
+            temp = entryArr[phi_s0 - 1];
 
             this->nameEntryVtx[phi_t1].v.ob[0] = this->nameEntryVtx[phi_t1 + 2].v.ob[0] =
-                D_808125EC[phi_s0] + this->nameEntryBoxPosX + D_808124C0[temp];
+                xPos[phi_s0] + this->nameEntryBoxPosX + fileNameCharWidth[temp];
 #else
             this->nameEntryVtx[phi_t1].v.ob[0] = this->nameEntryVtx[phi_t1 + 2].v.ob[0] =
-                D_808125EC[phi_s0] + this->nameEntryBoxPosX;
+                NameCharPosX[phi_s0] + this->nameEntryBoxPosX;
 #endif
 
             this->nameEntryVtx[phi_t1 + 1].v.ob[0] = this->nameEntryVtx[phi_t1 + 3].v.ob[0] =
                 this->nameEntryVtx[phi_t1].v.ob[0] + 0xA;
         } else {
             this->nameEntryVtx[phi_t1].v.ob[0] = this->nameEntryVtx[phi_t1 + 2].v.ob[0] =
-                D_808125EC[phi_s0] + this->nameEntryBoxPosX;
+                xPos[phi_s0] + this->nameEntryBoxPosX;
 
             this->nameEntryVtx[phi_t1 + 1].v.ob[0] = this->nameEntryVtx[phi_t1 + 3].v.ob[0] =
                 this->nameEntryVtx[phi_t1].v.ob[0] + 0xA;
@@ -335,18 +354,27 @@ void FileSelect_SetNameEntryVtx(GameState* thisx) {
 
     for (phi_s0 = 0, phi_v0 = 0; phi_s0 < 0x20; phi_s0 += 4, phi_v0++) {
         FileSelect_DrawCharacter(this->state.gfxCtx,
-                                 font->fontBuf + this->fileNames[this->buttonIndex][phi_v0] * FONT_CHAR_TEX_SIZE,
+                                 font->fontBuf + entryArr[phi_v0] * FONT_CHAR_TEX_SIZE,
+                                 phi_s0);
+    }
+
+    // randomizer vertices
+    gSPVertex(POLY_OPA_DISP++, this->nameEntryVtx + 4 + 32, 8, 0);
+
+    for (phi_s0 = 0, phi_v0 = 8; phi_s0 < 8; phi_s0 += 4, phi_v0++) {
+        FileSelect_DrawCharacter(this->state.gfxCtx,
+                                 font->fontBuf + entryArr[phi_v0] * FONT_CHAR_TEX_SIZE,
                                  phi_s0);
     }
 
-    this->nameEntryVtx[0x25].v.tc[0] = this->nameEntryVtx[0x26].v.tc[1] = this->nameEntryVtx[0x27].v.tc[0] =
-        this->nameEntryVtx[0x27].v.tc[1] = this->nameEntryVtx[0x29].v.tc[0] = this->nameEntryVtx[0x2A].v.tc[1] =
-            this->nameEntryVtx[0x2B].v.tc[0] = this->nameEntryVtx[0x2B].v.tc[1] = 0x300;
+    this->nameEntryVtx[0x25+8].v.tc[0] = this->nameEntryVtx[0x26+8].v.tc[1] = this->nameEntryVtx[0x27+8].v.tc[0] =
+        this->nameEntryVtx[0x27+8].v.tc[1] = this->nameEntryVtx[0x29+8].v.tc[0] = this->nameEntryVtx[0x2A+8].v.tc[1] =
+            this->nameEntryVtx[0x2B+8].v.tc[0] = this->nameEntryVtx[0x2B+8].v.tc[1] = 0x300;
 
     if (this->kbdButton == FS_KBD_BTN_HIRA || this->kbdButton == FS_KBD_BTN_KATA || this->kbdButton == FS_KBD_BTN_END) {
-        this->nameEntryVtx[0x29].v.tc[0] = this->nameEntryVtx[0x2B].v.tc[0] = 0x700;
+        this->nameEntryVtx[0x29+8].v.tc[0] = this->nameEntryVtx[0x2B+8].v.tc[0] = 0x700;
     } else if (this->kbdButton == FS_KBD_BTN_ENG || this->kbdButton == FS_KBD_BTN_BACKSPACE) {
-        this->nameEntryVtx[0x29].v.tc[0] = this->nameEntryVtx[0x2B].v.tc[0] = 0x500;
+        this->nameEntryVtx[0x29+8].v.tc[0] = this->nameEntryVtx[0x2B+8].v.tc[0] = 0x500;
     }
 
     CLOSE_DISPS(this->state.gfxCtx, "../z_file_nameset_PAL.c", 307);
@@ -569,30 +597,30 @@ void FileSelect_DrawNameEntry(GameState* thisx) {
     FileSelect_PulsateCursor(&this->state);
 
     tmp = (this->newFileNameCharCount * 4) + 4;
-    this->nameEntryVtx[36].v.ob[0] = this->nameEntryVtx[38].v.ob[0] = this->nameEntryVtx[tmp].v.ob[0] - 6;
-    this->nameEntryVtx[37].v.ob[0] = this->nameEntryVtx[39].v.ob[0] = this->nameEntryVtx[36].v.ob[0] + 24;
-    this->nameEntryVtx[36].v.ob[1] = this->nameEntryVtx[37].v.ob[1] = this->nameEntryVtx[tmp].v.ob[1] + 7;
-    this->nameEntryVtx[38].v.ob[1] = this->nameEntryVtx[39].v.ob[1] = this->nameEntryVtx[36].v.ob[1] - 24;
+    this->nameEntryVtx[36+8].v.ob[0] = this->nameEntryVtx[38+8].v.ob[0] = this->nameEntryVtx[tmp].v.ob[0] - 6;
+    this->nameEntryVtx[37+8].v.ob[0] = this->nameEntryVtx[39+8].v.ob[0] = this->nameEntryVtx[36+8].v.ob[0] + 24;
+    this->nameEntryVtx[36+8].v.ob[1] = this->nameEntryVtx[37+8].v.ob[1] = this->nameEntryVtx[tmp].v.ob[1] + 7;
+    this->nameEntryVtx[38+8].v.ob[1] = this->nameEntryVtx[39+8].v.ob[1] = this->nameEntryVtx[36+8].v.ob[1] - 24;
 
     if ((this->kbdButton == FS_KBD_BTN_HIRA) || (this->kbdButton == FS_KBD_BTN_KATA) ||
         (this->kbdButton == FS_KBD_BTN_END)) {
         if (this->kbdX != this->kbdButton) {
             PRINTF("014 xpos=%d  contents=%d\n", this->kbdX, this->kbdButton);
         }
-        this->nameEntryVtx[40].v.ob[0] = this->nameEntryVtx[42].v.ob[0] =
+        this->nameEntryVtx[40+8].v.ob[0] = this->nameEntryVtx[42+8].v.ob[0] =
             gNameEntryVtx[(this->kbdX + 1) * 4].v.ob[0] - 4;
-        this->nameEntryVtx[41].v.ob[0] = this->nameEntryVtx[43].v.ob[0] = this->nameEntryVtx[40].v.ob[0] + 52;
-        this->nameEntryVtx[40].v.ob[1] = this->nameEntryVtx[41].v.ob[1] =
+        this->nameEntryVtx[41+8].v.ob[0] = this->nameEntryVtx[43+8].v.ob[0] = this->nameEntryVtx[40+8].v.ob[0] + 52;
+        this->nameEntryVtx[40+8].v.ob[1] = this->nameEntryVtx[41+8].v.ob[1] =
             gNameEntryVtx[(this->kbdX + 1) * 4].v.ob[1] + 4;
 
     } else if ((this->kbdButton == FS_KBD_BTN_ENG) || (this->kbdButton == FS_KBD_BTN_BACKSPACE)) {
         if (this->kbdX != this->kbdButton) {
             PRINTF("23 xpos=%d  contents=%d\n", this->kbdX, this->kbdButton);
         }
-        this->nameEntryVtx[40].v.ob[0] = this->nameEntryVtx[42].v.ob[0] =
+        this->nameEntryVtx[40+8].v.ob[0] = this->nameEntryVtx[42+8].v.ob[0] =
             gNameEntryVtx[(this->kbdX + 1) * 4].v.ob[0] - 4;
-        this->nameEntryVtx[41].v.ob[0] = this->nameEntryVtx[43].v.ob[0] = this->nameEntryVtx[40].v.ob[0] + 40;
-        this->nameEntryVtx[40].v.ob[1] = this->nameEntryVtx[41].v.ob[1] =
+        this->nameEntryVtx[41+8].v.ob[0] = this->nameEntryVtx[43+8].v.ob[0] = this->nameEntryVtx[40+8].v.ob[0] + 40;
+        this->nameEntryVtx[40+8].v.ob[1] = this->nameEntryVtx[41+8].v.ob[1] =
             gNameEntryVtx[(this->kbdX + 1) * 4].v.ob[1] + 4;
     } else {
         if (this->charIndex >= 65) {
@@ -604,17 +632,17 @@ void FileSelect_DrawNameEntry(GameState* thisx) {
         this->nameEntryVtx[40].v.ob[0] = this->nameEntryVtx[42].v.ob[0] =
             this->keyboardVtx[this->charIndex * 4].v.ob[0] - 6;
 #else
-        this->nameEntryVtx[40].v.ob[0] = this->nameEntryVtx[42].v.ob[0] =
+        this->nameEntryVtx[40+8].v.ob[0] = this->nameEntryVtx[42+8].v.ob[0] =
             this->keyboardVtx[this->charIndex * 4].v.ob[0] - D_80812544[this->charIndex] - 6;
 #endif
-        this->nameEntryVtx[41].v.ob[0] = this->nameEntryVtx[43].v.ob[0] = this->nameEntryVtx[40].v.ob[0] + 24;
-        this->nameEntryVtx[40].v.ob[1] = this->nameEntryVtx[41].v.ob[1] =
+        this->nameEntryVtx[41+8].v.ob[0] = this->nameEntryVtx[43+8].v.ob[0] = this->nameEntryVtx[40+8].v.ob[0] + 24;
+        this->nameEntryVtx[40+8].v.ob[1] = this->nameEntryVtx[41+8].v.ob[1] =
             this->keyboardVtx[this->charIndex * 4].v.ob[1] + 6;
     }
 
-    this->nameEntryVtx[42].v.ob[1] = this->nameEntryVtx[43].v.ob[1] = this->nameEntryVtx[40].v.ob[1] - 24;
+    this->nameEntryVtx[42+8].v.ob[1] = this->nameEntryVtx[43+8].v.ob[1] = this->nameEntryVtx[40+8].v.ob[1] - 24;
 
-    gSPVertex(POLY_OPA_DISP++, &this->nameEntryVtx[36], 8, 0);
+    gSPVertex(POLY_OPA_DISP++, &this->nameEntryVtx[36+8], 8, 0);
     gDPPipeSync(POLY_OPA_DISP++);
     gDPSetCombineLERP(POLY_OPA_DISP++, 1, 0, PRIMITIVE, 0, TEXEL0, 0, PRIMITIVE, 0, 1, 0, PRIMITIVE, 0, TEXEL0, 0,
                       PRIMITIVE, 0);
@@ -647,32 +675,43 @@ void FileSelect_DrawNameEntry(GameState* thisx) {
                       PRIMITIVE, 0);
     gDPSetPrimColor(POLY_OPA_DISP++, 0, 0, 255, 255, 255, 255);
 
-    if (this->configMode == CM_NAME_ENTRY) {
+    if (this->configMode == CM_NAME_ENTRY || this->configMode == CM_SEED_ENTRY) {
+        u8* entryArr = &this->fileNames[this->buttonIndex][0];
+        s16 newStringMaxIndex = 7;
+        if (this->configMode == CM_SEED_ENTRY) {
+            entryArr = &this->seedChars[this->buttonIndex][0];
+            newStringMaxIndex = 9;
+        }
         if (CHECK_BTN_ALL(input->press.button, BTN_START)) {
             SFX_PLAY_CENTERED(NA_SE_SY_FSEL_DECIDE_L);
             // place cursor on END button
             this->kbdY = 5;
             this->kbdX = 4;
         } else if (CHECK_BTN_ALL(input->press.button, BTN_B)) {
-            if ((this->newFileNameCharCount == 7) && (this->fileNames[this->buttonIndex][7] != FILENAME_SPACE)) {
-                for (i = this->newFileNameCharCount; i < 7; i++) {
-                    this->fileNames[this->buttonIndex][i] = this->fileNames[this->buttonIndex][i + 1];
+            if ((this->newFileNameCharCount == newStringMaxIndex) && (entryArr[newStringMaxIndex] != FILENAME_SPACE)) {
+                for (i = this->newFileNameCharCount; i < newStringMaxIndex; i++) {
+                    entryArr[i] = entryArr[i + 1];
                 }
 
-                this->fileNames[this->buttonIndex][i] = FILENAME_SPACE;
+                entryArr[i] = FILENAME_SPACE;
                 SFX_PLAY_CENTERED(NA_SE_SY_FSEL_DECIDE_S);
             } else {
                 this->newFileNameCharCount--;
 
                 if (this->newFileNameCharCount < 0) {
                     this->newFileNameCharCount = 0;
-                    this->configMode = CM_NAME_ENTRY_TO_MAIN;
+                    if (this->configMode == CM_SEED_ENTRY) {
+                        this->seed[this->buttonIndex] = 0;
+                        this->configMode = CM_SEED_ENTRY_TO_RANDOMIZER;
+                    } else {
+                        this->configMode = CM_NAME_ENTRY_TO_MAIN;
+                    }
                 } else {
-                    for (i = this->newFileNameCharCount; i < 7; i++) {
-                        this->fileNames[this->buttonIndex][i] = this->fileNames[this->buttonIndex][i + 1];
+                    for (i = this->newFileNameCharCount; i < newStringMaxIndex; i++) {
+                        entryArr[i] = entryArr[i + 1];
                     }
 
-                    this->fileNames[this->buttonIndex][i] = FILENAME_SPACE;
+                    entryArr[i] = FILENAME_SPACE;
                     SFX_PLAY_CENTERED(NA_SE_SY_FSEL_DECIDE_S);
                 }
             }
@@ -849,23 +888,27 @@ void FileSelect_DrawNameEntry(GameState* thisx) {
                                              font->fontBuf + gCharPageEng[this->charIndex] * FONT_CHAR_TEX_SIZE, 0);
 
                     if (CHECK_BTN_ALL(input->press.button, BTN_A)) {
-                        SFX_PLAY_CENTERED(NA_SE_SY_FSEL_DECIDE_S);
-                        this->fileNames[this->buttonIndex][this->newFileNameCharCount] = gCharPageEng[this->charIndex];
-                        this->newFileNameCharCount++;
+                        if (this->configMode == CM_NAME_ENTRY || gCharPageEng[this->charIndex] <= 9 || gCharPageEng[this->charIndex] == FILENAME_SPACE) {
+                            SFX_PLAY_CENTERED(NA_SE_SY_FSEL_DECIDE_S);
+                            entryArr[this->newFileNameCharCount] = gCharPageEng[this->charIndex];
+                            this->newFileNameCharCount++;
 
-                        if (this->newFileNameCharCount > 7) {
-                            this->newFileNameCharCount = 7;
+                            if (this->newFileNameCharCount > newStringMaxIndex) {
+                                this->newFileNameCharCount = newStringMaxIndex;
+                            }
+                        } else {
+                            SFX_PLAY_CENTERED(NA_SE_SY_FSEL_ERROR);
                         }
                     }
                 } else if (CHECK_BTN_ALL(input->press.button, BTN_A) && (this->charPage != this->kbdButton)) {
                     if (this->kbdButton == FS_KBD_BTN_BACKSPACE) {
-                        if ((this->newFileNameCharCount == 7) &&
-                            (this->fileNames[this->buttonIndex][7] != FILENAME_SPACE)) {
-                            for (i = this->newFileNameCharCount; i < 7; i++) {
-                                this->fileNames[this->buttonIndex][i] = this->fileNames[this->buttonIndex][i + 1];
+                        if ((this->newFileNameCharCount == newStringMaxIndex) &&
+                            (entryArr[newStringMaxIndex] != FILENAME_SPACE)) {
+                            for (i = this->newFileNameCharCount; i < newStringMaxIndex; i++) {
+                                entryArr[i] = entryArr[i + 1];
                             }
 
-                            this->fileNames[this->buttonIndex][i] = FILENAME_SPACE;
+                            entryArr[i] = FILENAME_SPACE;
                             SFX_PLAY_CENTERED(NA_SE_SY_FSEL_DECIDE_S);
                         } else {
                             this->newFileNameCharCount--;
@@ -874,35 +917,138 @@ void FileSelect_DrawNameEntry(GameState* thisx) {
                                 this->newFileNameCharCount = 0;
                             }
 
-                            for (i = this->newFileNameCharCount; i < 7; i++) {
-                                this->fileNames[this->buttonIndex][i] = this->fileNames[this->buttonIndex][i + 1];
+                            for (i = this->newFileNameCharCount; i < newStringMaxIndex; i++) {
+                                entryArr[i] = entryArr[i + 1];
                             }
 
-                            this->fileNames[this->buttonIndex][i] = FILENAME_SPACE;
+                            entryArr[i] = FILENAME_SPACE;
                             SFX_PLAY_CENTERED(NA_SE_SY_FSEL_DECIDE_S);
                         }
                     } else if (this->kbdButton == FS_KBD_BTN_END) {
-                        validName = false;
+                        if (this->configMode == CM_NAME_ENTRY) {
+                            validName = false;
 
-                        for (i = 0; i < 8; i++) {
-                            if (this->fileNames[this->buttonIndex][i] != FILENAME_SPACE) {
-                                validName = true;
-                                break;
+                            for (i = 0; i < 8; i++) {
+                                if (this->fileNames[this->buttonIndex][i] != FILENAME_SPACE) {
+                                    validName = true;
+                                    break;
+                                }
                             }
-                        }
 
-                        if (validName) {
-                            SFX_PLAY_CENTERED(NA_SE_SY_FSEL_DECIDE_L);
-                            gSaveContext.fileNum = this->buttonIndex;
-                            dayTime = ((void)0, gSaveContext.save.dayTime);
-                            Sram_InitSave(this, &this->sramCtx);
-                            gSaveContext.save.dayTime = dayTime;
-                            this->configMode = CM_NAME_ENTRY_TO_MAIN;
-                            this->nameBoxAlpha[this->buttonIndex] = this->nameAlpha[this->buttonIndex] = 200;
-                            this->connectorAlpha[this->buttonIndex] = 255;
-                            Rumble_Request(300.0f, 180, 20, 100);
+                            if (validName) {
+                                SFX_PLAY_CENTERED(NA_SE_SY_FSEL_DECIDE_L);
+                                this->newFileNameCharCount = 0;
+                                gSaveContext.fileNum = this->buttonIndex;
+                                dayTime = ((void)0, gSaveContext.save.dayTime);
+                                Sram_InitSave(this, &this->sramCtx);
+                                gSaveContext.save.dayTime = dayTime;
+                                this->configMode = CM_NAME_ENTRY_TO_MAIN;
+                                this->nameBoxAlpha[this->buttonIndex] = this->nameAlpha[this->buttonIndex] = 200;
+                                this->connectorAlpha[this->buttonIndex] = 255;
+                                Rumble_Request(300.0f, 180, 20, 100);
+                            } else {
+                                SFX_PLAY_CENTERED(NA_SE_SY_FSEL_ERROR);
+                            }
                         } else {
-                            SFX_PLAY_CENTERED(NA_SE_SY_FSEL_ERROR);
+                            // Randomizer Seed Entry
+
+                            bool leadingZero = true;
+                            bool isNumber = true;
+                            // Remove leading zeroes and spaces
+                            for (i = 0; i < 10; i++) {
+                                if (this->seedChars[this->buttonIndex][i] != FILENAME_DIGIT('0') &&
+                                    this->seedChars[this->buttonIndex][i] != FILENAME_SPACE) {
+                                    if (this->seedChars[this->buttonIndex][i] <= FILENAME_DIGIT('9')) {
+                                        leadingZero = false;
+                                    }
+                                    continue;
+                                }
+                                if (this->seedChars[this->buttonIndex][i] == FILENAME_DIGIT('0') && !leadingZero) {
+                                    continue;
+                                }
+                                bool hasDigit = false;
+                                bool movedLastChar = false;
+                                for (int j = i; j < 10; j++) {
+                                    if (this->seedChars[this->buttonIndex][j] <= FILENAME_DIGIT('9')) {
+                                        hasDigit = true;
+                                    }
+                                    if (j == 9) {
+                                        if (this->seedChars[this->buttonIndex][j] != FILENAME_SPACE) {
+                                            movedLastChar = true;
+                                        }
+                                        this->seedChars[this->buttonIndex][j] = FILENAME_SPACE;
+                                    } else {
+                                        this->seedChars[this->buttonIndex][j] = this->seedChars[this->buttonIndex][j + 1];
+                                    }
+                                }
+                                if (hasDigit) {
+                                    if (!movedLastChar) {
+                                        this->newFileNameCharCount--;
+                                        if (this->newFileNameCharCount < 0) {
+                                            this->newFileNameCharCount = 0;
+                                        }
+                                    }
+                                    isNumber = true;
+                                    i--;
+                                } else {
+                                    break;
+                                }
+                            }
+
+                            bool exceedsU32 = this->seedChars[this->buttonIndex][0] > 4 && this->seedChars[this->
+                                                  buttonIndex][0] != FILENAME_SPACE &&
+                                              this->seedChars[this->buttonIndex][1] > 2 && this->seedChars[this->
+                                                  buttonIndex][1] != FILENAME_SPACE &&
+                                              this->seedChars[this->buttonIndex][3] > 4 && this->seedChars[this->
+                                                  buttonIndex][3] != FILENAME_SPACE &&
+                                              this->seedChars[this->buttonIndex][5] > 6 && this->seedChars[this->
+                                                  buttonIndex][5] != FILENAME_SPACE &&
+                                              this->seedChars[this->buttonIndex][6] > 7 && this->seedChars[this->
+                                                  buttonIndex][6] != FILENAME_SPACE &&
+                                              this->seedChars[this->buttonIndex][8] > 2 && this->seedChars[this->
+                                                  buttonIndex][8] != FILENAME_SPACE &&
+                                              this->seedChars[this->buttonIndex][9] > 5 && this->seedChars[this->
+                                                  buttonIndex][9] != FILENAME_SPACE;
+
+                            bool isRandom = true;
+                            for (i = 0; i < 10; i++) {
+                                if (this->seedChars[this->buttonIndex][i] != FILENAME_SPACE) {
+                                    isRandom = false;
+                                }
+                            }
+
+                            if (isNumber && !isRandom && !exceedsU32) {
+                                u32 newSeed = 0;
+                                int currentExp = 0;
+                                // Get seed from digits
+                                for (i = 0; i < 10; i++) {
+                                    if (this->seedChars[this->buttonIndex][9-i] == FILENAME_SPACE) {
+                                        continue;
+                                    }
+                                    int workExp = currentExp;
+                                    u32 workVal = 1;
+                                    while (workExp) {
+                                        workVal *= 10;
+                                        workExp--;
+                                    }
+                                    newSeed += (workVal * this->seedChars[this->buttonIndex][9-i]);
+                                    currentExp++;
+                                }
+                                if (newSeed == 0) {
+                                    Lib_MemSet(this->seedChars[this->buttonIndex], sizeof(this->seedChars[0]), FILENAME_SPACE);
+                                    this->newFileNameCharCount = 0;
+                                }
+                                this->seed[this->buttonIndex] = newSeed;
+                            } else if (isRandom) {
+                                this->seed[this->buttonIndex] = 0;
+                            }
+
+                            if ((isNumber || isRandom) && !exceedsU32) {
+                                this->configMode = CM_SEED_ENTRY_TO_RANDOMIZER;
+                                Rumble_Request(300.0f, 180, 20, 100);
+                            } else {
+                                SFX_PLAY_CENTERED(NA_SE_SY_FSEL_ERROR);
+                            }
                         }
                     }
                 }
@@ -911,8 +1057,8 @@ void FileSelect_DrawNameEntry(GameState* thisx) {
                     SFX_PLAY_CENTERED(NA_SE_SY_FSEL_CURSOR);
                     this->newFileNameCharCount++;
 
-                    if (this->newFileNameCharCount > 7) {
-                        this->newFileNameCharCount = 7;
+                    if (this->newFileNameCharCount > newStringMaxIndex) {
+                        this->newFileNameCharCount = newStringMaxIndex;
                     }
                 } else if (CHECK_BTN_ALL(input->press.button, BTN_CLEFT)) {
                     SFX_PLAY_CENTERED(NA_SE_SY_FSEL_CURSOR);
@@ -955,7 +1101,11 @@ void FileSelect_StartNameEntry(GameState* thisx) {
         this->kbdX = 0;
         this->kbdY = 0;
         this->kbdButton = FS_KBD_BTN_NONE;
-        this->configMode = CM_NAME_ENTRY;
+        if (this->configMode == CM_START_SEED_ENTRY) {
+            this->configMode = CM_SEED_ENTRY;
+        } else {
+            this->configMode = CM_NAME_ENTRY;
+        }
     }
 }
 
@@ -1296,11 +1446,12 @@ void FileSelect_UpdateOptionsMenu(GameState* thisx) {
     if (CHECK_BTN_ALL(input->press.button, BTN_B)) {
         SFX_PLAY_CENTERED(NA_SE_SY_FSEL_DECIDE_L);
         this->configMode = CM_OPTIONS_TO_MAIN;
-        sramCtx->readBuff[0] = gSaveContext.soundSetting;
-        sramCtx->readBuff[1] = gSaveContext.zTargetSetting;
+        sramCtx->readBuff[SRAM_HEADER_SOUND] = gSaveContext.soundSetting;
+        sramCtx->readBuff[SRAM_HEADER_Z_TARGET] = gSaveContext.zTargetSetting;
 #if OOT_PAL_N64
-        sramCtx->readBuff[2] = gSaveContext.language;
+        sramCtx->readBuff[SRAM_HEADER_LANGUAGE] = gSaveContext.language;
 #endif
+        sramCtx->readBuff[SRAM_HEADER_QUICKTEXT] = gSaveContext.quicktextSetting;
         PRINTF("ＳＡＶＥ");
         Sram_WriteSramHeader(sramCtx);
         PRINTF_COLOR_YELLOW();
@@ -1325,19 +1476,10 @@ void FileSelect_UpdateOptionsMenu(GameState* thisx) {
             if (gSaveContext.soundSetting > 0xF0) {
                 gSaveContext.soundSetting = SOUND_SETTING_SURROUND;
             }
-        } else {
-#if !OOT_PAL_N64
+        } else if (sSelectedSetting == FS_SETTING_TARGET) {
             gSaveContext.zTargetSetting ^= 1;
-#else
-            if (sSelectedSetting == FS_SETTING_TARGET) {
-                gSaveContext.zTargetSetting ^= 1;
-            } else {
-                gSaveContext.language--;
-                if (gSaveContext.language >= LANGUAGE_MAX) {
-                    gSaveContext.language = LANGUAGE_MAX - 1;
-                }
-            }
-#endif
+        } else {
+            gSaveContext.quicktextSetting ^= 1;
         }
     } else if (this->stickAdjX > 30) {
         SFX_PLAY_CENTERED(NA_SE_SY_FSEL_CURSOR);
@@ -1348,19 +1490,10 @@ void FileSelect_UpdateOptionsMenu(GameState* thisx) {
             if (gSaveContext.soundSetting > SOUND_SETTING_SURROUND) {
                 gSaveContext.soundSetting = SOUND_SETTING_STEREO;
             }
-        } else {
-#if !OOT_PAL_N64
+        } else if (sSelectedSetting == FS_SETTING_TARGET) {
             gSaveContext.zTargetSetting ^= 1;
-#else
-            if (sSelectedSetting == FS_SETTING_TARGET) {
-                gSaveContext.zTargetSetting ^= 1;
-            } else {
-                gSaveContext.language++;
-                if (gSaveContext.language >= LANGUAGE_MAX) {
-                    gSaveContext.language = 0;
-                }
-            }
-#endif
+        } else {
+            gSaveContext.quicktextSetting ^= 1;
         }
     }
 
@@ -1455,9 +1588,9 @@ static OptionsMenuTextureInfo sOptionsMenuHeaders[] = {
     { { gFileSelSOUNDENGTex, gFileSelSOUNDENGTex, gFileSelSOUNDFRATex }, 64, 16 },
     { { gFileSelZTargetingENGTex, gFileSelZTargetingGERTex, gFileSelZTargetingFRATex }, 64, 16 },
     { { gFileSelCheckBrightnessENGTex, gFileSelCheckBrightnessGERTex, gFileSelCheckBrightnessFRATex }, 128, 16 },
+    { { gOptionQuicktextENGTex, gOptionQuicktextENGTex, gOptionQuicktextENGTex }, 64, 16 },
     { { gFileSelLanguageENGTex, gFileSelLanguageGERTex, gFileSelLanguageFRATex }, 64, 16 },
 };
-
 static OptionsMenuTextureInfo sOptionsMenuSettings[] = {
     { { gFileSelStereoENGTex, gFileSelStereoENGTex, gFileSelStereoFRATex }, 48, 16 },
     { { gFileSelMonoENGTex, gFileSelMonoENGTex, gFileSelMonoENGTex }, 48, 16 },
@@ -1465,8 +1598,8 @@ static OptionsMenuTextureInfo sOptionsMenuSettings[] = {
     { { gFileSelSurroundENGTex, gFileSelSurroundENGTex, gFileSelSurroundENGTex }, 48, 16 },
     { { gFileSelSwitchENGTex, gFileSelSwitchGERTex, gFileSelSwitchFRATex }, 48, 16 },
     { { gFileSelHoldENGTex, gFileSelHoldGERTex, gFileSelHoldFRATex }, 48, 16 },
-    { { gFileSelSwitchENGTex, gFileSelSwitchGERTex, gFileSelSwitchFRATex }, 48, 16 },
-    { { gFileSelHoldENGTex, gFileSelHoldGERTex, gFileSelHoldFRATex }, 48, 16 },
+    { { gOptionOffENGTex, gOptionOffENGTex, gOptionOffENGTex}, 32, 16 },
+    { { gOptionOnENGTex, gOptionOnENGTex, gOptionOnENGTex}, 24, 16 },
 };
 
 #else
@@ -1761,6 +1894,30 @@ void FileSelect_DrawOptionsImpl(GameState* thisx) {
         gSP1Quadrangle(POLY_OPA_DISP++, vtx, vtx + 2, vtx + 3, vtx + 1, 0);
     }
 
+    for (i = 6; i < 8; i++, vtx += 4) {
+        int j = i - 6;
+        gDPPipeSync(POLY_OPA_DISP++);
+        if (j == gSaveContext.quicktextSetting) {
+            if (sSelectedSetting == FS_SETTING_QUICKTEXT) {
+                gDPSetPrimColor(POLY_OPA_DISP++, 0, 0, cursorPrimRed, cursorPrimGreen, cursorPrimBlue,
+                                this->titleAlpha[0]);
+                gDPSetEnvColor(POLY_OPA_DISP++, cursorEnvRed, cursorEnvGreen, cursorEnvBlue, 255);
+            } else {
+                gDPSetPrimColor(POLY_OPA_DISP++, 0, 0, 255, 255, 255, this->titleAlpha[0]);
+                gDPSetEnvColor(POLY_OPA_DISP++, 0, 0, 0, 255);
+            }
+        } else {
+            gDPSetPrimColor(POLY_OPA_DISP++, 0, 0, 120, 120, 120, this->titleAlpha[0]);
+            gDPSetEnvColor(POLY_OPA_DISP++, 0, 0, 0, 255);
+        }
+
+        gDPLoadTextureBlock(POLY_OPA_DISP++, sOptionsMenuSettings[i].texture[gSaveContext.language], G_IM_FMT_IA,
+                            G_IM_SIZ_8b, OPTIONS_MENU_TEXTURE_WIDTH(sOptionsMenuSettings[i]),
+                            OPTIONS_MENU_TEXTURE_HEIGHT(sOptionsMenuSettings[i]), 0, G_TX_NOMIRROR | G_TX_WRAP,
+                            G_TX_NOMIRROR | G_TX_WRAP, G_TX_NOMASK, G_TX_NOMASK, G_TX_NOLOD, G_TX_NOLOD);
+        gSP1Quadrangle(POLY_OPA_DISP++, vtx, vtx + 2, vtx + 3, vtx + 1, 0);
+    }
+
     gSPVertex(POLY_OPA_DISP++, gOptionsMenuBrightnessVtx, 8, 0);
     vtx = 0;
 #endif
@@ -1784,33 +1941,6 @@ void FileSelect_DrawOptionsImpl(GameState* thisx) {
 
     vtx += 4;
 
-#if OOT_PAL_N64
-    gSPVertex(POLY_OPA_DISP++, gOptionsMenuLanguageVtx, 12, 0);
-
-    for (i = 0, vtx = 0; i != LANGUAGE_MAX; i++, vtx += 4) {
-        gDPPipeSync(POLY_OPA_DISP++);
-
-        if (i == gSaveContext.language) {
-            if (sSelectedSetting == FS_SETTING_LANGUAGE) {
-                gDPSetPrimColor(POLY_OPA_DISP++, 0, 0, cursorPrimRed, cursorPrimGreen, cursorPrimBlue,
-                                this->titleAlpha[0]);
-                gDPSetEnvColor(POLY_OPA_DISP++, cursorEnvRed, cursorEnvGreen, cursorEnvBlue, 0xFF);
-            } else {
-                gDPSetPrimColor(POLY_OPA_DISP++, 0, 0, 255, 255, 255, this->titleAlpha[0]);
-                gDPSetEnvColor(POLY_OPA_DISP++, 0, 0, 0, 255);
-            }
-        } else {
-            gDPSetPrimColor(POLY_OPA_DISP++, 0, 0, 120, 120, 120, this->titleAlpha[0]);
-            gDPSetEnvColor(POLY_OPA_DISP++, 0, 0, 0, 255);
-        }
-
-        gDPLoadTextureBlock(POLY_OPA_DISP++, sLanguageChoices[i].texture, G_IM_FMT_IA, G_IM_SIZ_8b,
-                            sLanguageChoices[i].width, sLanguageChoices[i].height, 0, G_TX_NOMIRROR | G_TX_WRAP,
-                            G_TX_NOMIRROR | G_TX_WRAP, G_TX_NOMASK, G_TX_NOMASK, G_TX_NOLOD, G_TX_NOLOD);
-        gSP1Quadrangle(POLY_OPA_DISP++, vtx, vtx + 2, vtx + 3, vtx + 1, 0);
-    }
-#endif
-
     // blue divider lines
     gDPPipeSync(POLY_OPA_DISP++);
     gDPSetPrimColor(POLY_OPA_DISP++, 0, 0, 0, 255, 255, this->titleAlpha[0]);
@@ -1842,14 +1972,12 @@ void FileSelect_DrawOptionsImpl(GameState* thisx) {
     gSP1Quadrangle(POLY_OPA_DISP++, 0, 2, 3, 1, 0);
     Matrix_Pop();
 
-#if OOT_PAL_N64
     Matrix_Push();
     Matrix_Translate(0.0f, 0.8f, 0.0f, MTXMODE_APPLY);
     MATRIX_FINALIZE_AND_LOAD(POLY_OPA_DISP++, this->state.gfxCtx, "../z_file_nameset_PAL.c", UNK_LINE);
     gSPVertex(POLY_OPA_DISP++, gOptionsDividerLanguageVtx, 4, 0);
     gSP1Quadrangle(POLY_OPA_DISP++, 0, 2, 3, 1, 0);
     Matrix_Pop();
-#endif
 
     CLOSE_DISPS(this->state.gfxCtx, "../z_file_nameset_PAL.c", 1040);
 }
